#!/bin/bash -e

export DEBIAN_FRONTEND=noninteractive
sudo dpkg -i ethtool_5.4-1_amd64.deb || true 
sudo apt-get install ethtool || true
sudo ethtool -G eth0 rx 400 || true
sudo ethtool -G eth0 tx 400 || true

exit 0
